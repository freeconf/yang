module typedef {
    namespace "";
    prefix "";
    revision 0;

    typedef t {
        type string;
        default "hi";
    }

    typedef te {
        type enumeration {
            enum a;
            enum b;
        }
    }

    leaf x {
        type t;
    }

    leaf xe {
        type te;
    }
}